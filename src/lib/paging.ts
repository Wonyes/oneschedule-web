/**
 * 목록별 기본 페이지 크기.
 *
 * 서버의 @PageableDefault에 기대지 않고 프론트가 명시해서 보낸다.
 * 화면마다 한 번에 필요한 양이 다르고(카드 목록 vs 관리자 표), 여기 값만
 * 바꾸면 서버를 건드리지 않고 조절할 수 있다.
 *
 * 서버는 spring.data.web.pageable.max-page-size로 상한을 두고 있으니
 * 그보다 큰 값을 넣으면 조용히 깎인다.
 */
export const PAGE_SIZE = {
  /** 공개 그룹 탐색 — 카드가 커서 한 화면에 몇 개 안 들어간다 */
  publicGroups: 20,

  /** 가입 신청 — 관리자가 훑어보는 목록이라 넉넉히 */
  joinRequests: 5,

  /**
   * 내 그룹 — 그룹 전환기가 전부 있어야 고를 수 있어 한 번에 받는다.
   * 이만큼 가입할 일은 없다. 넘기 시작하면 무한 스크롤로 바꿔야 한다.
   */
  myGroups: 100,

  /*
   * 아래 둘은 서버 페이징이 아니라 이미 받아둔 배열을 몇 개씩 풀어놓는 단위다.
   * 나중에 서버 페이징으로 바꿔도 화면이 한 번에 보여주는 양은 그대로 쓰면 된다.
   */

  /** 그룹 멤버 */
  groupMembers: 5,

  /** 오늘 일정 */
  todaySchedules: 5,
} as const;
